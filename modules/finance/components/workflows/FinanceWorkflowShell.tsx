"use client";

import type { ReactNode } from "react";

export function FinanceWorkflowShell({ children }: { children: ReactNode }) {
  return <div className="finance-workflow-shell">{children}</div>;
}
