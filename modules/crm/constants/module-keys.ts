/** Module permissions ERP (`permissions.module_key`). */
export const CRM_MODULE_KEY = "crm" as const;

/** Aligné sur `profiles.department_key` pour opérateurs commerciaux. */
export const CRM_DEPARTMENT_KEY = "VENTE" as const;

/** Fil audit / gouvernance (routes admin filtre département). */
export const CRM_GOVERNANCE_DEPARTMENT_KEY = "VENTE" as const;
