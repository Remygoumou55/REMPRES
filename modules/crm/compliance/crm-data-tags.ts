/** Étiquettes conformité données CRM (PII / cycle de vie client). */
export const CRM_DATA_TAGS = {
  customerPii: "crm.customer.pii",
  pipelineFinancials: "crm.pipeline.financial_estimate",
  quoteDocument: "crm.quote.document",
} as const;
