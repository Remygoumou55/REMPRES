/** Réservé — branchement `erp_infrastructure_jobs` sans introduire de file parallèle. */
export const DASHBOARD_FOUNDATION_JOB_TYPES = {
  /** Futur : refresh matérialisé / snapshot analytics multi-domaines */
  foundationDigest: "dashboard_foundation.digest",
} as const;
