/** Clé module permissions / governance — alignée `permissions.module_key`. */
export const FINANCE_MODULE_KEY = "finance" as const;

/** Département entreprise (profiles.department_key uppercase). */
export const FINANCE_DEPARTMENT_KEY = "FINANCE" as const;
