# RUNTIME AUDIT REPORT — Étape 4

Baseline documentée dans `runtime-performance-registry.ts`.

Pipeline : layout (server cache) → Providers → AppShell (client) → children.

Dette identifiée : double sidebar mount, inline shellRail object, static CurrencySwitcher.
