/**
 * Pont analytics finance — réutilise les agrégations existantes (`lib/server/finance-overview`)
 * et étend progressivement vers métriques plan comptable / journal.
 */
export const FINANCE_ANALYTICS_BRIDGE_VERSION = 1;
