/**
 * Phase 1 : agrégation via les routes /api/dept/[deptKey]/kpis et le foundation engine.
 * Dépôt SQL direct réservé aux workers matérialisés.
 */
export const EXECUTIVE_READ_REPOSITORY_PLACEHOLDER = true;
