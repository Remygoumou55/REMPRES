/**
 * Dépenses opérationnelles : schéma historique `expenses` + RPC existantes.
 * Le domaine Enterprise étend via journal / budgets sans dupliquer les écrans dépenses.
 */
export const FINANCE_EXPENSES_ENTERPRISE_NOTE = "bridge-to-legacy-expenses";
